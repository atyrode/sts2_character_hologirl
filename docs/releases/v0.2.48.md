# Hologirl v0.2.48

- Sets `Linework No Ref 02` and the preferred tuner values as Hologirl's default character-select presentation.
- Replaces the runtime character-select button portrait with the selected Linework No Ref 02 close-up crop.
- Adds `Hologirl/audio/char_select/hologram.mp3` as Hologirl's character-select sound.
- Plays the character-select sound when Hologirl's character-select background scene is instantiated.
