# v0.2.27

## Changes

- Adds 20 additional generated character-select background variants, keeping the previous 10 variants available in the tuner.
- The new variants use simpler one-hue, two-to-three-value background plates to better match vanilla character-select art direction.
- Archives the new generated attempts and prompt constraints under `docs/design/art_archive/menu/character_select_background/generated-simple-variants-2026-05-15/`.
- Bumps the mod manifest to `v0.2.27`.

## Validation

- `scripts/package.sh`
- `scripts/godot-smoke-character-select.gd`
