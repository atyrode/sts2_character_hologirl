# Hologirl v0.2.38

- Adds a temporary puppet pose tuner for `Puppet Attempt 002`.
- Lets the tuner select individual puppet parts and adjust X, Y, rotation, and scale.
- Adds a motion toggle to freeze puppet animation while aligning parts.
- Adds `Reset Part` and `Copy Pose` controls for calibration.
- Extends the character-select smoke test to verify the puppet tuning section appears for the puppet variant.
