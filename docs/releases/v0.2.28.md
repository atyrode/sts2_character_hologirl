# v0.2.28

## Changes

- Replaces the rejected generated character-select background batches with five flat, bright, vanilla-style background plates.
- Keeps the character-select tuner background selector, now limited to `Cyan Lightning`, `Cobalt Flame`, `Rose Curtain`, `Mint Signal`, and `Gold Halo`.
- Adds `scripts/generate-character-bg-variants.sh` so the flat background batch can be regenerated deterministically.
- Updates character-select design notes to reject textured/painterly background variants and document the new bright solid-color motif direction.

## Validation

- `scripts/package.sh`
- `scripts/godot-smoke-character-select.gd`
