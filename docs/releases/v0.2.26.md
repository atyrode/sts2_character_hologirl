# Hologirl v0.2.26

- Replaces the temporary code-drawn geometric background variants with 10 generated bitmap character-select background variants.
- Bundles resized runtime backgrounds under `Hologirl/images/charui/background_variants/`.
- Archives original generated backgrounds and prompt notes under `docs/design/art_archive/menu/character_select_background/generated-variants-2026-05-15/`.
- Updates the background selector to load the generated background images in-game.
- Adds a `Collapse` / `Expand` button to the temporary character-select tuning panel.
- Extends the Godot character-select smoke test to verify generated background texture loading and the collapse button.
