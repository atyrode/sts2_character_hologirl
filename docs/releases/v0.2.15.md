# Hologirl v0.2.15

- Rebuilds the character-select screen as a layered scene: separate background plate, separate Hologirl foreground cutout, and code-drawn effects.
- Uses character-select cutout attempt 007 as the in-game foreground art.
- Adds a new characterless crystalline stage background for the character-select screen.
- Replaces bobbing static particles with short-lived emitter particles for golden whip sparkles.
- Adds blue hologram drift particles around Hologirl to make the character-select screen feel less static.
- Documents the new layered character-select direction and archives the generated background prompt/result.
