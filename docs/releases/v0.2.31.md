# Hologirl v0.2.31

- Sets the current preferred character-select defaults: `Periwinkle Drift`, `Chunky Vanilla`, X `842`, Y `120`, scale `1.51`, whip density `5.0`, drift density `1.3`, and gold jitter `80`.
- Adds a first conservative layered-motion attempt on the character-select scene: separate masked blue hologram and gold whip overlays drift subtly over the static cutout.
- Keeps the base character art static, avoiding the previous whole-image stretch effect.
- Documents why the tuner should not mutate the vanilla character-select button directly without a confirmed mod-safe API.
- Stores the manifest `version` value as `0.2.31` without a leading `v`; release tags remain `v0.2.31`.
