# v0.3.4

## Fixes

- Fixed the in-run `F3` debug panel toggle. The panel root now remains active for input while only the inner UI panel is hidden and shown.
- Root cause: `v0.3.3` hid the same `Control` node that owned the `F3` input handler, so the node could not receive the key press needed to show itself again.
