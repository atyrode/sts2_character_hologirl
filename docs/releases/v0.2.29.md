# Hologirl v0.2.29

- Replaces the character-select background picker entries with three new hand-painted-style plates: `Signal Bloom`, `Stage Glow`, and `Holo Drift`.
- Archives the generated originals and prompts under `docs/design/art_archive/menu/character_select_background/`.
- Updates character-select design notes to reject mechanically geometric motifs while keeping the validated bright, low-color, simple background direction.
