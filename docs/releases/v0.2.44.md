# Hologirl v0.2.44

- Replaces the previous thin-outline character-select variants with corrected versions that preserve the established `Chunky Vanilla` / `White Gold Blue` Hologirl identity.
- Keeps the close action composition, faceless eye-bar hologram face, large twin-tail silhouette, white/gold outfit, and golden light-whip direction.
- Changes the F3 tuner `Tear speed` control into `Tear freq`, which adjusts tear-band frequency/density.
- Keeps `Tear` as the horizontal tearing intensity control.
- Archives the corrected thin-outline batch and prompts in the design archive.
