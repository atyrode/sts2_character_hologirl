# Hologirl v0.2.23

- Repositions the temporary character-select tuning panel from actual viewport coordinates instead of scene-local coordinates.
- Counter-scales the tuning panel against the parent scene transform so it should keep a stable onscreen size.
- Disables root clipping while the temporary tuner is enabled so the panel can remain visible even if the character-select background root is offset by the game.
