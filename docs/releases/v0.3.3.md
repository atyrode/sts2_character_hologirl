# v0.3.3

## Added

- Added an in-run `F3` Hologirl debug panel for faster manual testing.
- The panel delegates to the game's dev console command backend for room, fight, event, and manual commands instead of reimplementing room or combat transitions.
- Added quick room buttons plus room, encounter, and event pickers backed by the current `ModelDb` entries.
