# Hologirl v0.3.10

- Expanded `Livestream` card text so it describes the new hype-trigger behavior.
- Added fallback localization aliases for the `Livestream` power title and descriptions to avoid runtime programmatic-name fallbacks.
- Made the Livestream chat overlay appear immediately when the `Livestream` card is played.
- Added stream-start chat messages to the Livestream overlay catalog.
