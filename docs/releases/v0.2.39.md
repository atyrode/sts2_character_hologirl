# Hologirl v0.2.39

- Removes the runtime `Puppet Attempt 002` character-select variant.
- Removes the packaged puppet layer assets from the mod runtime.
- Keeps the archived puppet attempts in `docs/` as design research only.
- Restores character select to the stable PNG cutout variants.
- Hides the character-select tuner by default and toggles it with `F3`.
- Updates smoke coverage for hidden tuner behavior and absence of runtime puppet nodes.
