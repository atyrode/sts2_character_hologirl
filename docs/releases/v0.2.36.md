# Hologirl v0.2.36

- Removes the rejected character-select body motion overlay from runtime.
- Removes the generated `Chunky Vanilla` runtime layer assets and layer-generation script that caused visible seams, overlaps, and cutoffs.
- Keeps character select on a stable single cutout with the existing particle effects and tuner.
- Archives two generated layered-puppet experiments under `docs/design/art_archive/menu/character_select_layers/`.
- Adds a dependency-free PNG splitter script for chroma-keyed rig-sheet experiments.
- Adds a Godot smoke test for the archived layered-puppet preview scene.
