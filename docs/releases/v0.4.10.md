# Hologirl v0.4.10

- Fixes the sheet F combat rig layer order so behind/body parts no longer use negative Godot `z_index` values.
- Regenerates the runtime layered rig scene to use sibling draw order instead of per-part `z_index`, keeping limbs out of reward UI layers after combat.
- Adds a lightweight part-level idle script for torso/head sway, hair lag, and arm/hand drift.
