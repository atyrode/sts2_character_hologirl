# v0.2.13

- Moves character-select particles into an explicit BaseLib foreground scene so they are not hidden by the background layer.
- Gives the particle node explicit viewport/container bounds instead of relying on inherited layout.
- Adds faint animated cyan hologram bands over Hologirl in addition to the gold whip sparkles.
