# Hologirl v0.2.19

- Moves Hologirl back onto the vanilla character-select path instead of the broken BaseLib custom-entry visual path.
- Adds a packed Godot character-select background scene under `Hologirl/scenes/character_select/`.
- Uses a simple two-tone background, chroma-keyed Hologirl layer, whip sparkles, and hologram drift inside the scene.
- Stops registering the previous custom character-select entry so the background should no longer draw over the character buttons.
