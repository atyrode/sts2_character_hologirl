# Hologirl v0.4.8

- Replaces the combat PNG bridge with the first runtime Spine-style multipart Hologirl visual for testing.
- Adds runtime Spine atlas, JSON skeleton, skeleton data, and `SpineSprite` scene resources under `Hologirl/animation/`.
- Keeps the source proof documented under `docs/design/experiments/spine-multipart-idle-proof/`.
- Fixes the JSON Spine import mapping to use `spine.json` rather than the binary `.skel` importer.
