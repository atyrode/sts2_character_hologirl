## Changes

- Replaced the in-game `Livestream` card art with the closer lofi-framed approved variant.
- Archived the new Livestream art as `docs/design/art_archive/cards/livestream/attempt-005/`.
- Added `prompt.md` files for all archived art attempts, using exact prompts where available and reconstructed/missing notes where not.
- Added reusable scripts for the art workflow:
  - `scripts/archive-art-attempt.sh`
  - `scripts/resize-card-art.sh`

## Verification

- Resized the selected Livestream art to `1000x760` and `250x190`.
- Syntax-checked the new scripts with `bash -n`.
- Built and packaged successfully with `scripts/release.sh`.
