# v0.3.24

- Adds a neutral username pass with ordinary viewer-style handles, numbers, underscores, and mixed casing so chat feels less STS2-reference-heavy.
- Adds dedicated emote-centered pile-on families, including `o7`/`F`/`HoloSalute` reactions for deaths and exhausts, `HoloClean` for clean defensive or recovery moments, `HoloPog`/`HoloHype` for big hits, and `HoloSweat`/`HoloPanic`/`HoloQuestion` for scary or uncertain turns.
