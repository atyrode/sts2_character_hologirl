# Hologirl v0.3.7

- Replaces the temporary `?` character icon with the selected vanilla-style Hologirl head symbol.
- Uses the same Hologirl head symbol for the map marker/current-position icon.
- Archives the selected icon source and documents the runtime copies.
