# Hologirl v0.3.11

- Fixed release packaging to export a fresh Godot PCK by default instead of reusing a stale quick-packer output.
- Verified the shipped PCK now contains the updated `Livestream` card and power localization.
- Raised the Livestream chat overlay layer and added DevMode-visible diagnostics when the overlay layer/node is created.
- Switched the Livestream chat rows from BBCode `RichTextLabel` rendering to plain Godot labels with per-user colors and text shadow for a simpler first visual draft.
