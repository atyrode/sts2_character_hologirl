# v0.3.25

- Removes the extra temporary `Concert!` from Hologirl's starting deck, leaving one `Concert!` plus the temporary `Livestream` testing card.
- Reworks `Livestream`, `Singing`, and `Fans` power icons with distinct color identities and clearer silhouettes:
  - `Singing`: magenta musical-note symbol with gold accent.
  - `Livestream`: cyan video/chat tile, with no broadcast-wave overlap.
  - `Fans`: gold audience/star motif.
- Keeps the power icons as transparent symbol-only assets with no backing plate or broad shadow.
- Refines Livestream chat pacing:
  - stream-start chat now uses a broader random opening wave and suppresses early unrelated generic chatter;
  - reaction bursts now use stochastic short/medium/long gaps instead of fixed step timing.
