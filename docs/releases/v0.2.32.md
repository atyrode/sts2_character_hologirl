# Hologirl v0.2.32

- Replaces the static vanilla character-select button portrait with the approved attempt 011 framing.
- Builds the portrait from the selected character-screen defaults: `Chunky Vanilla` over `Periwinkle Drift`.
- Adds `scripts/create-character-select-button.gd` so the approved portrait can be regenerated from source assets.
- Archives the character-select button exploration attempts and comparison strip under `docs/design/art_archive/menu/character_select_button/`.
