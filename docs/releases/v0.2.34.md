# Hologirl v0.2.34

- Adds the first generated transparent character-select layer set for the approved `Chunky Vanilla` art.
- Uses separate runtime overlay textures for the whip, ponytail regions, and visible arm region when `Chunky Vanilla` is selected.
- Keeps the full original character cutout as the stable base so imperfect automatic masks do not create holes.
- Keeps non-default character variants on the previous shader-mask fallback.
- Adds `scripts/create-character-select-layers.gd` and archives the generated layer attempt.
- Records tuner cleanup tasks for removing the redundant summary and fixing collapsed panel bounds.
