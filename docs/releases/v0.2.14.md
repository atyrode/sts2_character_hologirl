# v0.2.14

- Removes the cheap full-image idle bob/pulse from the character-select splash.
- Replaces the failed custom `_Draw` particle overlay with ordinary animated `ColorRect` particle nodes created from `RegisterScene`.
- Keeps particles in the same background scene as the splash so the implementation uses the hook that is confirmed to run.
