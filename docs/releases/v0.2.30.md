# Hologirl v0.2.30

- Adds nine more character-select background color candidates, keeping the validated `Signal Bloom` broad hand-painted motif style while covering more of the color wheel.
- Adds a `Character` selector to the in-game character-select tuner with `Current`, `Vanilla Matte`, `Chunky Vanilla`, and `Soft Vanilla` cutout variants.
- Rebuilds whip sparkle and hologram drift emitters from the selected character variant image so particles stay aligned while comparing cutouts.
- Documents `Signal Bloom` as the validated character-select background style anchor and notes its cyan/Defect overlap caveat.
