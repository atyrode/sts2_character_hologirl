# Hologirl v0.2.37

- Adds `Puppet Attempt 002` as an in-game character-select tuner variant.
- Packages the puppet layer PNGs under `Hologirl/images/charui/puppet_attempt_002/`.
- Keeps the stable `Chunky Vanilla` cutout as the default character-select variant.
- Adds runtime smoke coverage that switches to the puppet variant and verifies its layer sprites load.
