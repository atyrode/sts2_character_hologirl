# Hologirl v0.3.13

- Fixed Livestream chat overlay setup so it no longer depends on Godot `_Ready()` firing before the first chat message.
- Added synchronous overlay initialization before every message push.
- Kept DevMode diagnostics for overlay initialization and pushed chat rows.
