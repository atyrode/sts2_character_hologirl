# Hologirl v0.2.35

- Removes the redundant live values summary from the top of the character-select tuner.
- Fixes tuner collapse so the panel bounds shrink with the hidden content.
- Makes the `Chunky Vanilla` character-select motion visible by using the separated base cutout as the stable layer, then rendering the whip, ponytail, and arm layers at full opacity with slightly larger offsets.
- Keeps the print/copy tuning values button for reporting chosen settings.
- Adds a smoke-test assertion for the collapsed tuner panel height.
