# Broadcast Loop Power Icon Attempt 005

Generated for `BroadcastLoopPower`, because active Hologirl powers should ship with owned power icons instead of falling back to the generic placeholder.

Direction:

- Simple symbol-only Slay the Spire 2 style power icon.
- Warm amber-orange loop arrows with a small teal broadcast node.
- No text, frame, badge, background color, or shadow.
- Chroma-key source was keyed to transparent, then resized to `64x64` and `256x256`.

Prompt summary:

Create a simple hand-painted cel-shaded game power icon for `Broadcast Loop`: two imperfect circular arrows around a small broadcast signal node, warm amber-orange with a small teal accent, readable at small size, on flat `#00ff00` chroma-key background for removal.
